<?php

declare(strict_types=1);

namespace ElPandaPe\FilamentBouncer\Tests\Fixtures;

/**
 * An icon set shipped as a backed enum, which is the ordinary way to name one.
 */
enum ResourceIcon: string
{
    case Shield = 'lucide-shield';
}
